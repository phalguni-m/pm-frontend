import { PageHeader } from "@/components/primitives/PageHeader";

// Stub route — Members management is explicitly out of scope. See SCOPE.md.
export function MembersPage() {
  return <PageHeader title="Members" />;
}
