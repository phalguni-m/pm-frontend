import { PageHeader } from "@/components/primitives/PageHeader";

// Stub route — project-wide History is explicitly out of scope. See SCOPE.md.
export function HistoryPage() {
  return <PageHeader title="History" />;
}
